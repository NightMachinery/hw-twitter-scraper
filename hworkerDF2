FROM neo4j:3.5
WORKDIR /code
RUN apt-get update
RUN apt-get install -y python3 python3-dev zsh
COPY requirements.txt requirements.txt
RUN pip install --no-cache-dir -r requirements.txt
COPY . .

CMD zsh -c 'echo python: $(which python)'
